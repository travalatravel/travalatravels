# Clone Website

Clone a target URL using the **clone-website** skill from [JCodesMore/ai-website-cloner-template](https://github.com/JCodesMore/ai-website-cloner-template).

**Current clone target:** https://www.travala.com (see `TARGET.md`)

Invoke with a URL, e.g. `clone https://www.travala.com`. The agent loads `~/.cursor/skills/clone-website/SKILL.md` and follows the extract → spec → build → QA pipeline.
